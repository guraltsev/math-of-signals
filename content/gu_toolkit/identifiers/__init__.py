"""Canonical identifier validation, parsing, rendering, and symbol helpers.

Use this subpackage as the canonical home for identifier semantics. The
legacy module ``gu_toolkit.identifier_policy`` remains available as a
compatibility re-export.
"""

from .policy import *
from .policy import __all__
